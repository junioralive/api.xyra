{
    "name": "xyra-api",
    "version": "1.0.0",
    "main": "functions/api/doc.js",
    "scripts": {
        "dev": "wrangler pages dev public",
        "deploy": "wrangler pages deploy public",
        "audit": "npm audit"
    },
    "engines": {
        "node": ">=22"
    },
    "dependencies": {
        "axios": "^1.7.9",
        "cheerio": "^1.0.0",
        "dotenv": "^16.4.7"
    },
    "devDependencies": {
        "wrangler": "^4.26.0"
    }
}
